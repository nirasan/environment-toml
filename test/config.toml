user = "master user"
password = "master password"
max_connection = 100
show_slow_query = false
addresses = ["192.168.0.1", "192.168.0.2"]

[development]
password = "12345"
max_connection = 1
show_slow_query = true

[production]
timeout = 0.5
addresses = ["10.0.0.1", "10.0.0.2", "10.0.0.3"]

[postgres]
user = "postgresuser"
password = "mypassword"
tables = ["user", "password"]

[postgres.development]
user = "root"
password = ""
tables = ["user", "password", "debuglog"]

[postgres.production]
user = "rouser"
